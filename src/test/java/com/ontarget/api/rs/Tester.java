package com.ontarget.api.rs;

import java.text.SimpleDateFormat;
import java.util.Date;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.SerializationFeature;
import com.ontarget.request.bean.ProjectDetailInfo;
import com.ontarget.request.bean.ProjectRequest;

public class Tester {
	
	public static void main(String args[]) throws JsonProcessingException{
//		ObjectMapper mapper = new ObjectMapper();
//		mapper.configure(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS, true);
//        mapper.setDateFormat(new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss"));
//        mapper.configure(SerializationFeature.INDENT_OUTPUT, true);
//        ProjectRequest projectRequest = new ProjectRequest();
//        ProjectDetailInfo prjDetailInfo = new ProjectDetailInfo();
//        prjDetailInfo.setStartDate(new Date());
//        projectRequest.setProject(prjDetailInfo);
//        		
//        final String output = mapper.writeValueAsString(projectRequest);
//        System.out.println(output);
       
	}

}
